let typed = new Typed('#anim', {
    // Waits 1000ms after typing "First"
    strings: ['Photographer','Designer','Deveolper','Freelance'],
loop: true,
typeSpeed: 300,
backSpeed: 100,
backDelay: 50,
  });

  function test()
  {
    let x= 10;
    console.log(x);
  }
  test();
 